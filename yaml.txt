yaml: Yet Another Markup Language(仍是一种标记语言)

基本语法：
1. 大小写敏感
2. 使用缩进标识层级关系
3. 缩进不允许使用tab键，只允许空格
4. 缩进的空格数不重要，只要相同层级的元素左对齐即可
5. # 标识注释

数据类型：
1. 对象：键值对的集合，又称映射/哈希/字典
2. 数组：一组按照次序排列的值，又称为序列(sequence)/(list)
3. 纯数(scalars): 单个的，不可再分的值 
